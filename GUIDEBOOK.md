# Codex로 브랜드 홈페이지 만들기

## 저당퐁 프로젝트 실전 가이드북

이 문서는 아이디어만 있던 저당과자 브랜드를 순수 HTML/CSS/JS 홈페이지로 만들고, 제품·블로그·연락처·검색 최적화·창업자 소개까지 연결한 실제 과정을 재현할 수 있도록 정리한 가이드입니다.

> 핵심 원칙: 한 번에 완성하려 하지 말고, **페이지 골격 → 실제 데이터 → 콘텐츠 → 연결 → 디자인 통일 → 검색 설정 → 검증** 순서로 진행합니다.

---

## 1. 완성된 결과

현재 프로젝트에는 다음 기능이 들어 있습니다.

- 팝한 톤의 반응형 브랜드 홈페이지
- 인터뷰를 바탕으로 만든 창업자 소개 페이지
- `products.json`으로 관리하는 제품 목록
- `posts.json`으로 관리하는 블로그
- 블로그 관리 화면
- 이메일·Threads·스마트스토어 연락 페이지
- 홈페이지 최신 블로그 글 자동 연결
- 페이지별 제목과 설명, sitemap, robots, JSON-LD, llms.txt
- 모바일 햄버거 메뉴와 통일된 헤더·푸터

### 현재 파일 구조

```text
homepage-sample/
├── index.html                 # 홈페이지
├── about.html                 # 만든 사람 이야기
├── about.css
├── products.html              # 제품 목록
├── products.json              # 제품 원본 데이터
├── contact.html               # 연락하기
├── styles.css                 # 사이트 공통 디자인
├── script.js                  # 제품·블로그 연결 및 공통 동작
├── sitemap.xml
├── robots.txt
├── llms.txt
└── story/
    ├── index.html             # 블로그 목록
    ├── post.html              # 블로그 상세
    ├── posts.json             # 글 원본 데이터
    ├── story.css              # 블로그 디자인
    └── admin.html             # 블로그 관리
```

---

## 2. 시작 전에 준비할 정보

Codex에게 처음 요청하기 전에 아래 내용을 한곳에 모읍니다. 준비되지 않은 항목은 임시 문구로 시작해도 됩니다.

### 브랜드 기본 정보

- 브랜드 이름
- 무엇을 파는지
- 원하는 분위기와 피하고 싶은 분위기
- 필요한 페이지
- 연락처와 SNS
- 판매 링크

### 제품 정보

- 제품명
- 가격
- 이미지 원본 URL
- 실제 구매 URL
- 확인된 용량·원재료·영양정보

### 브랜드 이야기

- 왜 시작했는지
- 가장 힘들었던 순간
- 제품에서 타협하지 않는 기준
- 기억에 남는 고객
- 앞으로의 약속

### 첫 요청 예시

```text
내 브랜드 홈페이지를 만들어줘.

- 브랜드 이름:
- 판매 제품:
- 원하는 느낌:
- 페이지 구성:
- 순수 HTML/CSS/JS, 빌드 도구 없음
- 한국어, 모바일 대응
- 아직 없는 내용은 임시 문구 사용
```

요청은 **목표, 실제 데이터, 디자인 방향, 기술 조건, 완료 기준**을 분리해 쓰면 결과가 안정적입니다.

---

## 3. 전체 제작 순서

### 1단계. 저장소와 홈페이지 골격 만들기

처음에는 홈페이지, 소개 영역, 제품, 이야기, 연락하기의 위치부터 잡습니다.

이 단계의 완료 기준:

- 저장소 최상위에 `index.html`이 있다.
- `<meta charset="utf-8">`이 있다.
- 데스크톱과 모바일에서 레이아웃이 깨지지 않는다.
- 메뉴가 각 섹션 또는 페이지로 이동한다.

실제 커밋: `72b73ff Build 저당퐁 brand website`

### 2단계. 로컬 서버로 확인하기

제품과 블로그 데이터는 `fetch()`로 JSON을 읽습니다. HTML 파일을 더블클릭해 `file://`로 열면 브라우저 보안 정책 때문에 JSON이 보이지 않을 수 있습니다.

프로젝트 폴더에서 실행합니다.

```bash
python3 -m http.server 4173
```

브라우저 주소:

```text
http://localhost:4173/
```

`file:///.../index.html`과 `http://localhost:4173/`은 같은 파일처럼 보여도 실행 환경이 다릅니다. 제품이나 글이 한쪽에서만 보일 때 가장 먼저 주소를 확인합니다.

### 3단계. 제품 데이터를 JSON으로 분리하기

제품을 HTML에 직접 반복해서 적지 않고 `products.json`에 저장합니다.

```json
[
  {
    "name": "제품명",
    "price": 4200,
    "image": "https://example.com/product.jpg",
    "url": "https://example.com/buy"
  }
]
```

`script.js`가 이 파일을 읽어 홈페이지에는 앞에서부터 최대 3개, 제품 페이지에는 전체 제품을 표시합니다. 새 제품을 추가할 때는 JSON만 수정합니다.

실제 커밋: `b13c10e Connect Smart Store product catalog`

### 4단계. JSON 기반 블로그 붙이기

블로그는 다음 세 부분으로 구성합니다.

- `story/posts.json`: 글 데이터
- `story/index.html`: 글 목록
- `story/post.html?id=글ID`: 상세 글

글 데이터의 기본 형식:

```json
{
  "id": "unique-post-id",
  "title": "질문형 제목",
  "date": "2026-08-26",
  "summary": "목록과 검색 결과에 사용할 요약",
  "tags": ["태그1", "태그2"],
  "body": "본문"
}
```

실제 커밋: `cc4a8c0 Add JSON-powered story blog`

### 5단계. 검색되는 질문형 블로그 글 쓰기

제목은 고객이 실제로 검색할 질문으로 작성합니다.

```text
저당 간식은 무엇을 확인하고 골라야 할까요?
```

본문 끝 FAQ 형식:

```text
## 관련 FAQ

- Q. 질문 1 A. 짧은 답변 1
- Q. 질문 2 A. 짧은 답변 2
- Q. 질문 3 A. 짧은 답변 3
```

이 형식을 지키면 페이지가 FAQ 구조화 데이터를 자동 생성합니다.

실제 커밋: `f40f23f Add three factual FAQ blog posts`

### 6단계. 연락 페이지와 푸터 연결하기

이메일, SNS, 스토어 링크는 큰 버튼으로 만들고 외부 링크는 새 탭으로 엽니다.

```html
target="_blank" rel="noopener noreferrer"
```

같은 연락처를 홈페이지, 제품, 블로그, 소개 페이지의 푸터에도 일관되게 표시합니다.

실제 커밋: `e9543bb Add branded contact page and footer links`

### 7단계. 검색엔진과 AI가 이해할 정보 정리하기

적용 항목:

- 페이지별 고유 `<title>`
- 페이지 내용을 요약한 meta description
- canonical URL
- `sitemap.xml`
- `robots.txt`
- 브랜드·제품 JSON-LD
- FAQ JSON-LD
- `llms.txt`

구조화 데이터에는 화면에 실제로 보이는 정보만 넣습니다. 확인하지 못한 재고, 리뷰, 영양성분, 제조 방식은 만들지 않습니다.

실제 커밋: `19a9f22 Add SEO metadata and structured data`

### 8단계. 홈페이지와 블로그를 실제 데이터로 연결하기

홈페이지의 이야기 카드가 고정 문구라면 블로그에 글을 추가해도 바뀌지 않습니다. `story/posts.json`을 읽어 최신 글 3개를 날짜순으로 표시하도록 연결합니다.

완료 기준:

- 최신 글 3개가 홈페이지에 보인다.
- 카드의 제목·날짜·요약이 JSON과 같다.
- 카드 클릭 시 정확한 상세 글로 이동한다.

실제 커밋: `75540d8 Connect homepage to blog posts`

### 9단계. 블로그 디자인과 네비게이션 통일하기

외부 템플릿을 붙이면 기능은 동작해도 홈페이지와 전혀 다른 사이트처럼 보일 수 있습니다. 블로그 목록과 상세 페이지에 공통 로고, 메뉴, 푸터, 색상, 서체를 적용합니다.

읽는 페이지는 화려함보다 다음을 우선합니다.

- 본문 폭 제한
- 충분한 줄 간격
- 제목과 본문의 명확한 위계
- 이전·다음 글 이동
- 모바일 햄버거 메뉴

실제 커밋: `66733dd Unify blog with site design`

### 10단계. 인터뷰로 ‘내 소개’ 만들기

소개 페이지는 보도자료처럼 쓰지 않고 창업자의 실제 말을 수집한 뒤 만듭니다.

인터뷰 질문:

1. 왜 이 일을 시작했는가? 당시의 구체적인 장면은?
2. 가장 힘들었던 순간은?
3. 제품에서 절대로 타협하지 않는 한 가지는?
4. 기억에 남는 고객 한 사람은?
5. 앞으로의 약속은?

답변을 바탕으로 다음 순서로 구성합니다.

- 한 문장 헤드라인
- 대표 사진
- 1인칭 창업 이야기
- 고객 인용문
- 과정 타임라인
- 마지막 약속

실제 커밋: `66c67e7 Add founder story page`

---

## 4. Codex에서 필요한 스킬

Codex의 **스킬**은 특정 작업을 위한 실행 지침 묶음입니다. 요청에 스킬 이름을 직접 넣거나, 해당 작업을 요청하면 Codex가 알맞은 스킬을 선택합니다.

### 확인하고 설치하기

- **확인**: Codex 입력창에 `$`를 입력하면 사용 가능한 스킬 목록이 나옵니다. `/skills`도 같습니다.
- **설치**: `$skill-installer <스킬이름>`
- 설치 후에는 **Codex를 재시작**해야 새 스킬을 인식합니다.
- **비활성화**: `~/.codex/config.toml`에 아래를 추가합니다.

```toml
[[skills.config]]
path = "/path/to/skill/SKILL.md"
enabled = false
```

### 기본 탑재 스킬 (설치 불필요)

Codex에 함께 배포되는 system 스킬입니다. 누구나 바로 쓸 수 있습니다.

| 스킬 | 사용하는 순간 |
|---|---|
| `imagegen` | 로고, 배너, 대표 이미지를 생성할 때 |
| `skill-installer` | 다른 스킬을 설치할 때 |
| `skill-creator` | 반복하는 작업을 나만의 스킬로 만들 때 |
| `plugin-creator` | 스킬을 플러그인으로 묶어 배포할 때 |
| `openai-docs` | Codex 사용법 자체를 Codex에게 물을 때 |

### 필요할 때 설치하는 스킬

`openai/skills` 저장소의 curated 목록에 있는 스킬입니다.

| 스킬 | 사용하는 순간 | 설치 |
|---|---|---|
| `playwright` | 실제 브라우저에서 기능과 반응형 화면을 검증할 때 | `$skill-installer playwright` |
| `screenshot` | 화면을 캡처해 디자인을 점검할 때 | `$skill-installer screenshot` |
| `vercel-deploy` | 배포와 도메인 연결 문제를 풀 때 | `$skill-installer vercel-deploy` |
| `pdf` | 제품 소개서나 성분표 PDF에서 정보를 뽑을 때 | `$skill-installer pdf` |

### 이 프로젝트에서 실제로 사용한 것

- **`playwright`**: 메뉴, 카드 이동, 데스크톱과 390px 모바일 화면을 확인했습니다.
- **`frontend-design`**: 홈페이지 톤을 블로그와 소개 페이지로 확장할 때 사용했습니다. 다만 이 스킬은 OpenAI 카탈로그가 아니라 [Anthropic 스킬 저장소](https://github.com/anthropics/skills/tree/main/frontend-design)에서 가져와 `~/.codex/skills/`에 둔 것입니다. **다른 사람의 Codex에는 없으므로 `$skill-installer frontend-design`은 동작하지 않습니다.**
- **7단계(검색엔진·AI 설정)는 스킬 없이 프롬프트만으로** 처리했습니다. 메타데이터, sitemap, robots, JSON-LD, llms.txt 전용 스킬을 쓴 것이 아닙니다.
- **`imagegen`은 사용하지 않았습니다.** 생성 이미지보다 실제 제품 사진으로 교체하는 편이 우선이기 때문입니다.

### 스킬이 동작하는 환경

| 환경 | 단독 스킬 |
|---|---|
| ChatGPT 데스크톱 앱 · Codex CLI · IDE 확장 | 동작합니다 |
| 브라우저(chatgpt.com)에서 실행하는 Codex | 지원 범위가 달라 `$` 목록이 비어 보일 수 있습니다 |

브라우저에서 Codex를 쓴다면 **스킬 이름 대신 지침을 프롬프트에 직접 적으세요.** 이 가이드북의 프롬프트가 그렇게 쓰여 있습니다.

### 스킬 요청 예시

```text
playwright로 데스크톱과 390px 모바일에서 메뉴와 페이지 이동을 검증해줘.
```

```text
imagegen으로 브랜드 로고를 만들어서 assets 폴더에 넣고 모든 페이지 헤더에 적용해줘.
```

스킬이 없을 때는 원하는 결과와 넣을 위치를 직접 말합니다.

```text
모든 페이지에 고유한 title과 meta description을 넣고
sitemap.xml과 robots.txt를 만들어줘.
story/admin.html은 sitemap에서 빼고 noindex, nofollow를 적용해줘.
확인되지 않은 리뷰·별점·영양정보는 구조화 데이터에 넣지 마.
```

스킬을 많이 쓰는 것보다 **현재 작업에 필요한 최소한의 스킬을 정확히 선택하는 것**이 중요합니다.

---

## 5. Codex에게 일을 잘 시키는 방법

### 한 번에 한 단계씩 요청하기

좋은 순서:

1. 골격을 만든다.
2. 실제 제품을 연결한다.
3. 블로그를 붙인다.
4. 콘텐츠를 쓴다.
5. 전체 디자인을 통일한다.
6. 검색 설정을 한다.
7. 브라우저에서 검증한다.

한 요청에 홈페이지, 쇼핑몰, 회원가입, 결제, 블로그, 검색 최적화를 모두 넣으면 검증할 범위가 너무 커집니다.

### 결과가 아니라 완료 조건을 말하기

```text
홈페이지에 최신 글 3개가 날짜순으로 보이고,
카드를 누르면 해당 상세 글로 이동하게 해줘.
모바일과 데스크톱에서 확인해줘.
```

### 실제 데이터를 붙여넣기

제품명, 가격, 연락처, 고객의 말은 추측하게 하지 말고 원문을 제공합니다. Codex에게 “확인되지 않은 내용은 쓰지 말라”고 함께 알려주면 과장을 줄일 수 있습니다.

### 디자인 피드백은 구체적으로 말하기

```text
어색해요
```

보다 아래처럼 말하는 편이 좋습니다.

```text
블로그만 다른 템플릿처럼 보입니다.
홈페이지와 같은 로고, 메뉴, 오렌지·옐로 색상, 푸터를 적용하고
본문은 읽기 편한 폭을 유지해 주세요.
```

---

## 6. 반드시 유의할 점

### 1. `file://`로 열지 않기

JSON을 읽는 기능은 로컬 서버에서 확인해야 합니다. 제품이나 글이 안 보이면 코드보다 먼저 주소가 `http://localhost:4173/`인지 확인합니다.

### 2. 로컬 미리보기와 공개 배포는 다르다

로컬에서 잘 보인다고 검색엔진이 접근할 수 있는 것은 아닙니다. 현재 저장소의 GitHub Pages는 아직 활성화되지 않았습니다. 공개 주소가 정해지면 canonical, sitemap, robots, llms.txt의 기준 주소도 함께 확인해야 합니다.

### 3. JSON 문법을 깨뜨리지 않기

쉼표 하나가 빠지거나 마지막 항목 뒤에 불필요한 쉼표가 있으면 제품 또는 글 전체가 보이지 않을 수 있습니다. 수정 후 JSON 파싱 검증을 합니다.

### 4. 상대 경로를 페이지 위치에 맞추기

루트 페이지에서는 `products.html`, 블로그 폴더에서는 `../products.html`을 사용합니다. 폴더 깊이를 무시하면 메뉴와 CSS가 끊깁니다.

### 5. 건강 효능을 추측하지 않기

저당, 무당, 저칼로리 같은 표현은 실제 제품 표시와 관련 기준을 확인해야 합니다. 고지혈증을 치료하거나 예방한다고 표현하지 않습니다. 개인의 경험과 제품의 의학적 효능은 구분합니다.

### 6. 고객 개인정보 보호하기

고객 사례에는 동의받지 않은 실명, 연락처, 주문정보를 공개하지 않습니다. 이 프로젝트에서는 `김○○`으로 익명화했습니다.

### 7. 관리자 토큰을 노출하지 않기

`story/admin.html`은 GitHub 토큰을 브라우저 localStorage에 저장하는 방식입니다. 공용 컴퓨터에서 사용하지 말고, 토큰을 대화·스크린샷·저장소에 올리지 않습니다. 최소 권한과 만료일을 설정합니다.

### 8. 외부 이미지 주소에 의존하는 위험 알기

스마트스토어 이미지 주소는 제공처 정책에 따라 차단되거나 변경될 수 있습니다. 장기 운영 시 사용 권한을 확인한 이미지를 저장소 또는 이미지 호스팅에 직접 올리는 편이 안전합니다.

### 9. 구조화 데이터는 보이는 내용과 같아야 한다

JSON-LD에만 가격, 리뷰, FAQ를 넣고 화면에는 표시하지 않으면 안 됩니다. 검색 노출과 리치 결과는 구조화 데이터를 넣었다고 보장되지 않습니다.

### 10. 코드 완료, 커밋, 배포를 구분하기

- 코드 수정 완료: 로컬 파일이 바뀐 상태
- 커밋 완료: Git 기록에 저장된 상태
- 푸시 완료: GitHub에 올라간 상태
- 배포 완료: 공개 URL에서 새 버전이 열리는 상태

각 단계는 서로 다른 완료 상태입니다.

### 11. 브라우저 캐시 확인하기

코드를 바꿨는데 이전 화면이 보이면 강력 새로고침을 하거나 새 탭에서 확인합니다. JSON 요청은 `cache: 'no-store'`를 사용해도 HTML과 CSS는 캐시될 수 있습니다.

### 12. 관리자 화면은 검색 대상에서 제외하기

관리 페이지에는 `noindex, nofollow`를 유지하고 sitemap에 포함하지 않습니다. `robots.txt`만으로 민감한 페이지를 보호할 수는 없으며, 실제 보안이 필요하면 인증을 붙여야 합니다.

---

## 7. 콘텐츠를 추가하는 방법

### 새 제품 추가

1. `products.json`을 연다.
2. 배열 안에 제품 객체를 추가한다.
3. 제품명, 가격, 이미지, 구매 URL을 확인한다.
4. 로컬 서버에서 홈페이지와 제품 페이지를 확인한다.

### 새 블로그 글 추가

1. `story/posts.json`을 연다.
2. 중복되지 않는 `id`를 만든다.
3. 날짜를 `YYYY-MM-DD`로 입력한다.
4. 제목, 요약, 태그, 본문을 입력한다.
5. FAQ가 있다면 정해진 문법을 지킨다.
6. 블로그 목록, 상세 글, 홈페이지 최신 글을 확인한다.

### 대표 사진 교체

현재 `about.html`에는 대표 사진 자리 표시가 있습니다. 실제 사진 파일을 받은 뒤 저장소에 추가하고 placeholder를 `<img>`로 교체합니다.

---

## 8. 검증 체크리스트

### 기능

- [ ] 홈페이지가 열린다.
- [ ] 제품이 JSON과 동일하게 표시된다.
- [ ] 구매 링크가 새 탭에서 열린다.
- [ ] 블로그 목록과 상세 글이 열린다.
- [ ] 홈페이지 최신 글 카드가 상세 글로 연결된다.
- [ ] 모든 페이지의 메뉴와 푸터 링크가 동작한다.
- [ ] 모바일 햄버거 메뉴가 열린다.

### 콘텐츠

- [ ] 제품명·가격·용량이 실제 정보와 같다.
- [ ] 확인되지 않은 영양정보나 효능이 없다.
- [ ] 고객 정보가 익명화되어 있다.
- [ ] 소개 페이지가 1인칭 말투로 읽힌다.

### 검색

- [ ] 공개 페이지마다 고유 title과 description이 있다.
- [ ] sitemap에 새 공개 페이지가 들어 있다.
- [ ] 관리자 페이지는 sitemap에서 제외되어 있다.
- [ ] JSON-LD가 실제 화면 내용과 일치한다.
- [ ] 공개 도메인이 정해진 뒤 canonical URL을 다시 확인했다.

### 운영

- [ ] `git status`에 예상하지 않은 파일이 없다.
- [ ] 변경사항을 커밋했다.
- [ ] GitHub에 푸시했다.
- [ ] 공개 배포 주소에서 다시 확인했다.

---

## 9. 다음에 할 수 있는 작업

- 실제 창업자 사진으로 교체
- 제품 영양정보와 원재료를 확인한 뒤 상세 페이지 강화
- GitHub Pages 또는 별도 호스팅으로 공개
- 실제 도메인에 맞춰 canonical·sitemap·robots·llms 주소 변경
- Search Console 등록과 sitemap 제출
- 관리자 페이지에 안전한 인증 적용
- 제품별 개별 상세 페이지 추가
- 블로그 카테고리와 검색 기능 추가

---

## 10. 가장 중요한 한 문장

**Codex로 홈페이지를 만드는 핵심은 멋진 첫 화면을 한 번에 얻는 것이 아니라, 실제 데이터가 연결된 각 페이지를 하나씩 만들고 실제 브라우저에서 끝까지 확인하는 것입니다.**
